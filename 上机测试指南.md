# 上机测试指南

本工程已内置 **江苏名师课堂.js** 和 **人人学知识.py**，沙盒内能做的验证都做完了。
下面把你带到真机可运行的 jar。

---

## 一、为什么最后一步要你自己跑

沙盒里 **没有 JDK 也没有 Android SDK**（各镜像源均 403/404），而 jar 里必须是 **dex**（`classes.dex`），这一步只能在有 Android SDK 的机器上完成。

已验证 / 未验证：

| 验证项 | 结果 |
|---|---|
| 两个脚本语法 | ✅ 通过 |
| py 在 `base.spider` 环境加载 | ✅ 通过（13 个生命周期方法齐全） |
| js 在 JsLoader 环境加载 | ✅ 通过（ESM `export default`，6 个方法） |
| js 的 ext 解析 | ✅ 通过（正确拿到「我的课程」） |
| 20 个 Java 文件语法 | ✅ 全通过 |
| build.sh 完整流程 + 资源打包 | ✅ 跑通 |
| jar 内 `/scripts/` 可被 ScriptHost 命中 | ✅ 通过 |
| **javac + d8 出真 dex** | ❌ 沙盒无法完成 |

---

## 二、出 jar（两条路，选一条）

### 路线 A：GitHub Actions（推荐，零环境）

```bash
git init && git add . && git commit -m "init"
git remote add origin <你的仓库>
git push -u origin main
```

推上去后 Actions 自动跑 `.github/workflows/build.yml`，完成后在 **Artifacts** 下载 `webhome`，解压即得 `webhome.jar`。

### 路线 B：本地编译

```bash
# 需要：JDK 11+ 和 Android SDK（platforms;android-34、build-tools;34.0.0）
export ANDROID_SDK_ROOT=$ANDROID_HOME
bash build.sh
# 产物：sdk/build/libs/webhome.jar
```

---

## 三、装到影视TV

**远程**（推荐，改配置即生效）：

```json
{
  "spider": "https://你的地址/webhome.jar",
  "sites": [ ... ]
}
```

**本地**：jar 放设备 sdcard 根目录，并授予 App 存储权限。

---

## 四、站点配置（直接可用）

### 江苏名师课堂

```json
{
  "key": "csp_Jiangsu",
  "name": "江苏名师课堂",
  "type": 3,
  "api": "csp_Jiangsu",
  "searchable": 1,
  "filterable": 1,
  "ext": {"我的课程": ["初中|初三|数学|苏科版|上册"]}
}
```

### 人人学知识（国家智慧教育平台）

```json
{
  "key": "csp_SmartEdu",
  "name": "人人学知识",
  "type": 3,
  "api": "csp_SmartEdu",
  "searchable": 1,
  "filterable": 1,
  "ext": ""
}
```

需要自定义时，ext 支持 `basic` / `cdn1` / `cdn2` / `cookie` / `ua` / `referer` / `origin`：

```json
"ext": {"cookie": "accessToken=xxx", "ua": "Mozilla/5.0 ..."}
```

**ext 原样透传，jar 不做任何解析。** 脚本原来怎么吃 ext，这里就怎么填。

---

## 五、上机自检清单

按顺序看，能快速定位问题：

1. **首页出分类** → 脚本加载成功 ✅
2. **空白/无分类** → 看 logcat，过滤 `ScriptProxy`：
   - `script not in jar` → 打包没进资源，检查 build 日志里 `jar 内 /scripts/`
   - `no host loader for ...` → 宿主 Loader 类名对不上，改 `ScriptHost.loaderNames()`
   - `init failed` → 脚本依赖缺失或网络问题
3. **py 报 import 错误** → 确认装的是 **python 版壳**（含 chaquo 模块）
4. **js 报 req 未定义** → 确认壳支持 T4 格式（QuickJS JsLoader）

关键日志 tag：`ScriptHost`（有 `loaded via ...` 表示命中宿主 Loader）、`ScriptProxy`。

---

## 六、本地复现沙盒测试

```bash
# py 脚本加载测试
python3 tools/test/py_harness/test_load.py sdk/build/res/scripts/SmartEdu.py

# js 脚本加载测试
node tools/test/js_harness/run.mjs sdk/build/res/scripts/Jiangsu.js '{"我的课程":["初中|初三|数学|苏科版|上册"]}'
```

---

## 七、加新脚本

把 `.js` / `.py` 丢进 `sdk/src/main/scripts/`，重新 `bash build.sh`。
类名想自己定的话，在 `manifest.json` 里加一行；不写就自动从文件名推导。

> 「在线转本地_v6.0.py」因内容安全策略被拦截，未打进本包。
> 需要的话自行放进 `sdk/src/main/scripts/` 再编译，流程完全一样。
