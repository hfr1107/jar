# jar 反编译防护

## 先说结论：天花板在哪

**dex 无法真正防住反编译。** jadx 打开 `classes.dex` 一键出 Java，这是 dex 格式决定的。

更关键的是：**传统 APK 加壳（DexGuard / 梆梆 / 360）对 spider.jar 基本不可用**。
因为加壳需要自定义 ClassLoader 在运行时解密还原，而这里的 ClassLoader 是**影视TV 壳创建的**，我们插不上手。

所以能做的只有三件事，按性价比排序：

| 措施 | 挡住谁 | 成本 |
|---|---|---|
| **脚本加密** | 解压 jar 直接看 js/py 明文 | 低（已实现） |
| **代码混淆** | 反编译后读懂逻辑 | 低（已集成） |
| **字符串加密** | 一眼看到 API 地址 / token | 中（需手动） |

真正的"保密逻辑"只有一条路：**写进 Java 层并混淆**，别放在脚本里（脚本运行时必然还原成明文）。

---

## 零、方案 A：加密 dex 动态加载（推荐，已实现）

**思路**：jar 里的 `classes.dex` 只放空壳，真实逻辑全部塞进加密的 `payload.bin`，运行时解密再用 `DexClassLoader` 加载。

```
jar/
├── classes.dex     ← 只有入口壳，jadx 打开啥也看不到
├── payload.bin     ← 加密的真实 dex（ScriptProxy/ScriptHost/真实爬虫…）
└── scripts/        ← 内置脚本
```

反编译者打开 `classes.dex` 只能看到：

```java
public class Jiangsu extends SecureSpider {
    protected String target() { return "com.github.catvod.spider.JiangsuScript"; }
}
```

### 用法

```bash
SECURE=1 bash build.sh
# 结束应看到：加密dex: 已启用
```

可与混淆叠加（先混淆真实 dex，再加密）：`SECURE=1` + 装好 R8。

### 两个必须知道的实现细节

**1. DexClassLoader 的 parent 必须是壳的 loader**

```java
new DexClassLoader(dexPath, optDir, null,
        SecureSpider.class.getClassLoader());   // ← 不能传 null
```

传 `null` 的话 payload 里的类找不到 `com.github.catvod.crawler.Spider`，直接 `NoClassDefFoundError`。

**2. 脚本要预释放**

payload 里的 `ScriptHost` 由 DexClassLoader 加载，它的 `getResourceAsStream` 查的是 `payload.dex` 所在目录，**拿不到 jar 内的 `scripts/`**。
所以 `SecureSpider` 会按 `ScriptIndex.PATHS` 先把脚本释放到 `filesDir`。

### 防护等级

和脚本加密同级：**防静态解压，防不住 root**。dex 解密后要落盘才能被加载。

---

## 一、代码混淆（已集成到 build.sh）

### 为什么必须小心

壳加载 jar 的方式是：

```java
loader.loadClass("com.github.catvod.spider." + clsKey)  // clsKey = api 去掉 csp_
    .newInstance() 后强转成它自己的 Spider，再调 sp.homeContent(filter)
```

这意味着**对外契约一个字都不能动**：

- 包名 `com.github.catvod.spider` + 类名 `Jiangsu` —— 壳按名反射
- `homeContent` / `categoryContent` 等方法名 —— 虚方法分派，改名就调不通
- `siteKey` 字段 —— 壳赋值
- `@JavascriptInterface` 方法 —— 名字写死在 JS 里

`sdk/proguard-rules.pro` 已经把这些全部 keep 住了，**只混淆内部实现**。

### 用法

**装 R8**（推荐，自带 dex 输出，一步到位）：

```bash
# 放到 tools/r8.jar 即可自动识别
curl -Lo tools/r8.jar https://repo1.maven.org/maven2/com/android/tools/r8/8.2.47/r8-8.2.47.jar
# 或指定路径
export R8_JAR=/path/to/r8.jar
```

**或用 ProGuard**（老 Android SDK 自带 `tools/proguard/lib/proguard.jar`，会自动探测）。

然后正常构建：

```bash
bash build.sh
# 看到 "混淆: 已启用" 即生效
```

构建结束时如果没装工具，会打印醒目警告 `混淆: 未启用`，不会静默跳过。

**临时关闭**：`OBFUSCATE=0 bash build.sh`

### 效果与风险

- ✅ 收益：内部类/方法/字段名变 a.b.c，反编译后可读性大幅下降
- ⚠️ 风险：keep 规则写漏会导致**加载失败或某个入口空白**

出问题时先 `OBFUSCATE=0` 构建一次对比，确认是混淆引起的再调 `proguard-rules.pro`。

---

## 二、脚本加密（已实现，默认关闭）

js/py 是明文躺在 `jar/scripts/` 里的，解压即拿。

```bash
# 先构建一次生成 res
bash build.sh

# 加密
python3 tools/encrypt_scripts.py

# 重新构建，让加密后的脚本打进 jar
bash build.sh
```

`ScriptHost` 靠文件头魔数 `WHS1` 自动识别，加密/明文都兼容，不用改任何配置。
调试时 `python3 tools/encrypt_scripts.py --decrypt` 可还原。

**诚实说明**：运行时必须还原成明文写到 `filesDir` 交给宿主 Loader，
所以这**只能防静态扒取**，防不住 root 后捞文件。要真保密就把逻辑写进 Java。

---

## 三、字符串加密（收益最大，需手动）

混淆改不了字符串内容。jadx 里搜 `https://` 一眼看到所有接口地址——这往往是**最值钱的泄露**。

ProGuard/R8 不做字符串加密，推荐做法：

```java
// 不要这样
private static final String API = "https://api.example.com/v1/list";

// 这样（密钥也混淆，两层）
private static String api() {
    return new String(xor(Base64.decode(S, 0), k()), "UTF-8");
}
```

或者接入字符串加密插件（`stringfog` 等 Gradle 插件，本工程的 bash 构建需手动集成）。

优先级：**先加密接口地址、签名密钥、token**，普通日志字符串不用管。

---

## 四、还有几件低成本的事

1. **删掉调试信息**：`proguard-rules.pro` 里去掉 `-keepattributes ...LineNumberTable`，
   反编译后没有行号，断点调试和阅读都变难（代价：自己排查也变难）
2. **移除日志**：打开配置里 `-assumenosideeffects android.util.Log` 那段
3. **加 md5 校验**：构建已自动生成 `webhome.jar.md5`，远程加载配置写成
   `"spider": "http://xxx/webhome.jar;md5;<md5>"`，
   既能校验完整性，也能防中间人替换
4. **反调试/环境检测**（可选）：在 `ScriptProxy.init()` 里检测
   `Debug.isDebuggerConnected()`、常见 hook 框架包名，命中就返回空数据

---

## 五、现实建议

如果目的是**防止别人直接白嫖整个 jar**（改个名字二次分发），
混淆 + 脚本加密 + 字符串加密这套组合已经够用——门槛高到大多数人会放弃。

如果目的是**防止专业逆向**，那在 spider.jar 这个形态下做不到。
真有强需求，应该把核心逻辑放到自建服务端，jar 只做转发。

另外，法律手段（License / 水印）常常比技术手段更有效：
在脚本输出里埋一个不易发现的标识，发现盗版时能取证。
